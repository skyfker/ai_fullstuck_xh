const config = {
    database: {
        DATABASE:'note-book',
        USERNAME:'root',
        PASSWORD:'123456',
        HOST:'localhost',
        PORT:3306
    }
}

module.exports = config  // commonJS风格模块导出